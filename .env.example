# OpenAI-compatible API - used ONLY by scripts 07 (task synthesis) and 14
# (LLM-judge failure analysis). The CASS method itself never calls the API.
# Copy this file to .env and fill in your own values.
API_BASE_URL=https://your-endpoint.example.com/compatible-mode/v1
API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
API_MODELS=model-a,model-b,model-c
